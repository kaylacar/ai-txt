{
  "name": "ai-txt",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "check:no-legacy-runner": "node ./scripts/check-no-legacy-runner.mjs",
    "test": "npm run check:no-legacy-runner && pnpm -r --workspace-concurrency=1 test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
